# Project Agent Notes

This repository is a Bun and TypeScript web application template.

Keep the template generic. Replace sample behavior only when the generated project has a concrete product purpose.
