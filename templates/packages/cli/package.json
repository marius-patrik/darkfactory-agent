{
  "name": "@agent-os/template-cli",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "bin": {
    "agent-os-template-cli": "./dist/index.js"
  },
  "scripts": {
    "build": "tsc -p tsconfig.build.json",
    "dev": "bun run src/index.ts",
    "test": "bun test src",
    "typecheck": "tsc -p tsconfig.json --noEmit"
  }
}
