{
  "schemaVersion": 1,
  "run": {
    "id": "run-20260713-verify-080",
    "kind": "verify",
    "purpose": "verification",
    "triggeredBy": "schedule"
  },
  "repository": {
    "owner": "marius-patrik",
    "repo": "DarkFactory",
    "defaultBranch": "dev"
  },
  "workItem": {
    "kind": "pr",
    "number": 80,
    "author": "darkfactory-bot",
    "url": "https://github.com/marius-patrik/DarkFactory/pull/80",
    "title": "Add prompt content for the auditor role",
    "body": "Implements #58. Needs independent verification.",
    "comments": []
  },
  "draftIntent": null,
  "policy": {
    "branching": "One worker = one issue = one branch = one PR; branch df/<issue>-<slug> from dev.",
    "labels": ["P0", "P1", "P2", "df:ready", "df:running", "df:blocked"],
    "enforcement": "All merges require green CI and the configured review gate; never force-push or bypass gates."
  },
  "validation": {
    "commands": ["npm run check"]
  },
  "effort": "low",
  "verified": {
    "facts": ["The PR branch is pushed and CI is configured."]
  },
  "output": { "id": "output/verifier" },
  "selection": {
    "role": "role/verifier",
    "skills": ["skill/verification-first", "skill/untrusted-input-handling"],
    "modelTier": "medium",
    "overlays": ["overlay/github-control-plane"]
  }
}
