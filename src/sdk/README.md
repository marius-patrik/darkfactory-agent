# sdk

The core package everything is implemented through.

Types, receipts, client bindings, the plugin contract, and the shared implementation surface that servers and clients build on. A pure library: no daemon, no state, no side effects.
