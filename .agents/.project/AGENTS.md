# agents-mono Agent Notes

This repository is the monorepo root. Keep root workflows and managed enforcement files at the root, and do not bump submodule gitlinks unless the task explicitly requires it.
