# Commands

- `python -m uv sync --locked`
- `python -m uv run ruff check agentos_gateway tests`
- `python -m uv run mypy agentos_gateway`
- `python -m uv run pytest -q`
- `python -m uv run python scripts/packaging_smoke.py`
