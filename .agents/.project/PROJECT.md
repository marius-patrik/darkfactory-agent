# Agents Core

Agents Core is a shared contracts package for Agentos protocol definitions and generated clients.

Primary surfaces:
- protobuf sources in `proto/`
- generated Go contracts in `contracts-go/`
- generated TypeScript stubs in `clients/shared-ts/src/gen/`
- protocol documentation in `docs/contracts/`
