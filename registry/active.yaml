schema_version: gateway-active-v1
# No role is pinned out-of-box: role-aliased traffic resolves by round-robin
# over the enabled local replicas of that role (router._resolve_role). An
# operator may pin a role with `gateway model-select <role> <model_id>` or the
# /roles/model endpoint.
active:
  general: null
  coding: null
  conversation: null
  judge: null
  embedding: null
