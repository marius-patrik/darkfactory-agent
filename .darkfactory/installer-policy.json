{
  "schemaVersion": 1,
  "installer": {
    "required": true,
    "paths": ["install/install.sh"],
    "postInstallChecks": ["state-init", "doctor"],
    "notes": "install/install.sh performs the canonical source install: pinned dev checkout, recursive pinned components, dependencies, one launcher, capability activation, provider pinning, and a strict state doctor."
  },
  "autoUpdater": {
    "centralized": true,
    "workflow": ".github/workflows/dark-factory-autoupdate.yml",
    "source": "marius-patrik/agents-data",
    "notes": "DarkFactory performs central managed setup sync through its GitHub App. Per-repo updater workflows verify that managed files are present and current enough to receive updates."
  }
}
