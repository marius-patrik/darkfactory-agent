# Agents Core Project Rules

Use the managed global rules first, then this project context.

This repository owns shared Agentos wire contracts, generated Go and TypeScript clients, and protocol documentation. Keep validation wired through the repository scripts in `package.json`.
