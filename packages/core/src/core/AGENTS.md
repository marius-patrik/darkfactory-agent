# Core component guidance

Use only `.agents/.project/` for this component's repository-specific context.
Shared identity, memory, roles, and skills come from the canonical `$AGENTS_HOME` installation.
