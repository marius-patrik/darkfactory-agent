# Commands

Install dependencies:

```sh
bun install --frozen-lockfile
```

Validate:

```sh
bun run check
```

Focused commands:

```sh
bun run buf:lint
bun run codegen:check
bun run go:test
bun run go:build
bun run ts:typecheck
```
