# Dream Agent Instructions

This repository contains the standalone Dream plugin. Follow the shared workflow
from `.agents/.global/` and keep changes scoped to this repository.

Do not mutate a real Codex memory root during validation unless the issue or PR
explicitly requires it. Use temporary memory roots for smoke tests.
