{
  "schemaVersion": 1,
  "id": "llm-gateway",
  "name": "LLM Gateway",
  "kind": "agent",
  "description": "OpenAI-format LLM gateway with registry routing, fallback, switchers, quota, OAuth dispatch seams, and gateway tests.",
  "provides": ["llm-gateway", "model-routing", "switchers"]
}
