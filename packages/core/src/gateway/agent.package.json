{
  "schemaVersion": 1,
  "id": "agent-os-gateway",
  "name": "Agent OS Gateway",
  "kind": "package",
  "description": "Local OpenAI-format model gateway with source-owned routing, health, quota windows, and private traces.",
  "entry": "uv run agent-os-gateway serve",
  "workingDirectory": ".",
  "requires": {
    "state": ["runtime/gateway"]
  },
  "provides": ["agent-os-gateway", "model-routing"]
}
