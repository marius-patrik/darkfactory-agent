---
description: Run Rommie sleep mode to consolidate stale memory and reduce context rot
argument-hint: [FOCUS="<optional memory area or project>"]
---

Use $sleep to run a long-term Rommie memory hygiene pass.

Focus: $FOCUS

Inspect the layered memory store, consolidate active and stale entries, refresh summaries where needed, preserve useful evidence pointers, avoid storing secrets, and write the sleep marker when the pass is complete.

