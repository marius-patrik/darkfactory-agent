{
  "name": "@agent-os/web",
  "private": true,
  "version": "0.10.0",
  "description": "Private placeholder workspace for the Agent OS web client.",
  "type": "module",
  "scripts": {
    "typecheck": "tsc -p ../../tsconfig.json --noEmit"
  }
}
