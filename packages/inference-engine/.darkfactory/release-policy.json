{
  "schemaVersion": 1,
  "release": {
    "packageManager": "auto",
    "required": {
      "validate": true,
      "build": false,
      "smoke": false
    },
    "scripts": {
      "validate": ["validate", "validate:fast", "ci", "check", "test"],
      "build": ["build:release", "release:build", "build"],
      "smoke": ["smoke:release", "release:smoke", "test:release"]
    },
    "artifactGlobs": [],
    "notes": "This repository currently publishes validation-only GitHub releases with generated release notes. Add build and smoke scripts plus artifact globs before documenting or requiring binary release payloads."
  }
}
