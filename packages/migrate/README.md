# migrate

The previous implementation, frozen for migration.

Code leaves this folder by being reimplemented against the sdk in its target component, never by being re-imported and never by being deleted. Nothing outside migrate may depend on it.
