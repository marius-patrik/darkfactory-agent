{
  "name": "@marius-patrik/agents-manager",
  "version": "0.2.0",
  "description": "Bun TypeScript CLI for managing agents-mono packages and shared local agent state.",
  "type": "module",
  "bin": {
    "agents": "./src/cli.ts"
  },
  "scripts": {
    "check": "bunx tsc --noEmit",
    "test": "bun test test/*.test.ts",
    "ci": "bun run check && bun run test"
  },
  "devDependencies": {
    "@types/bun": "latest",
    "typescript": "^5.5.0"
  },
  "engines": {
    "bun": ">=1.1.0"
  },
  "license": "MIT"
}
