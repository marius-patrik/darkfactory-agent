# plugins

Capabilities loaded through the sdk plugin contract.

A plugin extends behaviour without being part of the product core: it is discovered, loaded, and bounded by the plugin contract rather than imported directly by a component.
