# Hooks

Repository-owned hooks are rooted here. Each direct child must be a complete,
independently owned hook capability with its canonical manifest and entrypoint.

The current shared floor defines no repository hook payloads; provider-owned
system hooks remain inside their opaque provider homes.
