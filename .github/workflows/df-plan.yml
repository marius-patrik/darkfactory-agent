name: DarkFactory Plan

on:
  push:
    branches:
      - main
    paths:
      - 'PRD.md'
      - '**/PRD.md'
  schedule:
    - cron: "17 */6 * * *"
  workflow_dispatch:
    inputs:
      repo:
        description: Target repository in owner/name form. Defaults to this repository.
        required: false
        type: string
      ref:
        description: Optional target repository ref. Defaults to the target repository default branch.
        required: false
        type: string

permissions:
  contents: read

jobs:
  df-plan:
    name: Reconcile PRD backlog
    runs-on: ubuntu-latest
    if: >-
      ${{
        github.event_name == 'push' ||
        github.repository == 'marius-patrik/darkfactory-agent'
      }}

    steps:
      - name: Validate trusted control ref
        shell: bash
        run: |
          # Privileged planning mints an installation token, so it only runs
          # from a trusted default branch in an allowed owner.
          if [[ "${GITHUB_REPOSITORY_OWNER}" != "marius-patrik" ]]; then
            echo "::error::DarkFactory privileged planning can only run in the marius-patrik owner."
            exit 1
          fi

          if [[ "${GITHUB_REF_TYPE:-}" != "branch" || "${GITHUB_REF_NAME}" != "main" ]]; then
            echo "::error::DarkFactory privileged planning can only run from the main branch."
            exit 1
          fi

      - name: Resolve canonical control ref
        id: control-ref
        shell: bash
        run: |
          # Pin to an immutable SHA on marius-patrik/darkfactory-agent main so
          # PRD edits in managed repositories cannot execute arbitrary code.
          sha="$(curl -fsSL https://api.github.com/repos/marius-patrik/darkfactory-agent/commits/main | jq -r '.sha')"
          if [[ -z "${sha}" || "${sha}" == "null" ]]; then
            echo "::error::Could not resolve canonical DarkFactory control ref."
            exit 1
          fi
          echo "sha=${sha}" >> "$GITHUB_OUTPUT"

      - name: Checkout DarkFactory control scripts
        uses: actions/checkout@v4
        with:
          # Always run the canonical control scripts from marius-patrik/darkfactory-agent,
          # never from a managed repository's own checkout, so a compromised managed
          # repo cannot execute privileged planning code with the app token.
          repository: marius-patrik/darkfactory-agent
          ref: ${{ steps.control-ref.outputs.sha }}
          path: darkfactory-control
          persist-credentials: false

      - name: Mint mp-agents installation token
        id: app-token
        uses: actions/create-github-app-token@v2
        with:
          app-id: ${{ secrets.DARK_FACTORY_APP_ID }}
          private-key: ${{ secrets.DARK_FACTORY_PRIVATE_KEY }}
          owner: marius-patrik

      - name: Run planner
        env:
          DARK_FACTORY_TOKEN: ${{ steps.app-token.outputs.token }}
          DF_CONTROL_REPO: marius-patrik/darkfactory-agent
          DF_TARGET_REPO: ${{ github.event_name == 'workflow_dispatch' && inputs.repo || github.repository }}
          DF_TARGET_REF: ${{ github.event_name == 'workflow_dispatch' && inputs.ref || (github.event_name == 'push' && github.sha || '') }}
          DF_PLAN_ALL: ${{ github.event_name == 'schedule' && 'true' || 'false' }}
          DF_TRIGGER: ${{ github.event_name }}
          DF_DATA_REPO: marius-patrik/darkfactory-data
        run: node darkfactory-control/.github/scripts/df-plan.mjs
