{
  "name": "@marius-patrik/agents-manager",
  "version": "0.1.0",
  "description": "Agent OS manager and unified `agents` CLI for canonical state, memory, providers, sessions, orchestration, packages, and harnesses.",
  "type": "module",
  "workspaces": [
    "packages/core",
    "packages/core/src/core/clients/*"
  ],
  "bin": {
    "agents": "./packages/core/src/manager/cli.ts"
  },
  "scripts": {
    "agents": "bun packages/core/src/manager/cli.ts",
    "check": "bunx tsc --noEmit",
    "test": "bun test packages/core/test/manager/*.test.ts",
    "proto:check": "bun scripts/verify-codegen.ts",
    "ci": "bun run check && bun run test && bun run proto:check && node --test .github/scripts/run-kimi-review.test.mjs",
    "smoke:release": "bash install/smoke-release.sh"
  },
  "devDependencies": {
    "@bufbuild/buf": "1.71.0",
    "@types/bun": "1.3.14",
    "typescript": "5.9.3"
  },
  "engines": {
    "bun": "1.3.14"
  },
  "license": "MIT"
}
