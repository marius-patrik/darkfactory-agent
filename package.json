{
  "name": "@marius-patrik/agents-mono",
  "version": "0.1.0",
  "description": "Root aggregator workspace for the agents-mono topology. Exposes the `agents` CLI and validates the `packages/core` TypeScript test surface.",
  "type": "module",
  "bin": {
    "agents": "./packages/core/src/manager/cli.ts"
  },
  "scripts": {
    "check": "bunx tsc --noEmit",
    "test": "bun test packages/core/test/manager/*.test.ts",
    "ci": "bun run check && bun run test",
    "smoke:release": "bash install/smoke-release.sh"
  },
  "devDependencies": {
    "@types/bun": "latest",
    "typescript": "^5.5.0"
  },
  "engines": {
    "bun": ">=1.1.0"
  },
  "license": "MIT"
}
